1. $ git credential-cache exit = serve somente para o "git config --global credential.helper cache", para o keyring basta estar trocando o repo e tentar subir

2. Links Simbólicos funcionam com Scripts? Yes

3. A conta do root naturalmente vem desativada (sem senha), para ativa-la coloque uma senha para ele com o "passwd"

4. como restar a senha do root (https://sempreupdate.com.br/como-recuperar-a-senha-root-no-linux/)

5. Se você criar um link simbolico numa pasta normal de um arquivo depois criar outra pasta colocar no path e mover o link simbolico (que é executavel) para essa pasta ele passa virar o programa em si??

6. Eescrevendo temas zsh https://blog.carbonfive.com/writing-zsh-themes-a-quickref/ // %~ para pegar o path com a home?

7. mudar versão corrente do java // $ sudo update-alternatives --config java

8. /var/lib/apt/lists/ => usado (pelo apt)para armazenar informação para cada pacote
	/var/cache/apt/archives/ => onde os arquivos baixados dos repositórios ficam
	/var/lib/dpkg/ => usado (pelo dpkg)para armazenar informação para cada pacote
	/etc/apt/sources.list => repositórios
	/etc/apt/sources.list.d => adicionar repositorios sem precisar editar o /etc/apt/sources.list
